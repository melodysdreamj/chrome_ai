name: chrome_ai
description: "Support for chrome AI in Flutter. This package is a wrapper for the Chrome AI API. It provides a simple way to use the Chrome AI API in Flutter."
version: 0.0.9
homepage: https://github.com/melodysdreamj/chrome_ai

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  js: ^0.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
